# Roomcast 진행 상태

다른 세션 모니터링용. 단계 완료 시마다 갱신.

## 체크리스트

- [x] ① 스캐폴드 + 디자인 토큰(CSS 변수) + 상단바/뷰 탭 — 빌드 통과 (2026-08-24)
- [x] ② 1a 2D 에디터 — 빌드 통과 + 헤드리스 브라우저 검증 (2026-08-24)
  - 벽·룸·개구부(문 스윙 호/창)·외곽 치수선·룸 라벨 SVG 렌더
  - 가구 심볼 11종(top-view), 러그<가구<조명 z-순서
  - 카탈로그 패널(카테고리 pill·배치중 뱃지·클릭 배치 고스트), 인스펙터(X/Y/회전/높이 편집·스와치·가격), 툴독, 상태바(축척·스냅 토글·배치율)
  - 인터랙션: 선택/이동(그리드 10cm+벽면 12px 스냅, 정렬선+"벽에 붙임"+여유 라벨), 회전(15° 링, Shift 프리), 코너 리사이즈, 충돌(SAT, 경고 칩+드롭 후 "빈 자리로 이동/그대로 두기"), 벽 그리기(15°/10cm 스냅, 닫으면 룸 생성), 문/창 추가, 치수 측정, 팬(Space/휠클릭)·커서 기준 휠 줌
  - 단축키: V/W/D/N/M, Delete, Cmd+D, Cmd+Z/Shift+Z, 방향키 10cm(Shift 1cm), Esc
  - undo/redo 명령 단위, localStorage 2s 디바운스 자동저장("N분 전 저장")
- [x] ③ 1d 3D 워크스루 — 빌드+테스트 통과, HUD 브라우저 검증 (2026-08-24)
  - Plan→3D 파생: 룸 바닥(Shape)·벽 개구부 분할(문 상인방/창 sill·head·유리)·천장·가구 프리미티브 11종·램프 PointLight
  - PointerLockControls + WASD(1.4/Shift 3.0 m/s), 캡슐 0.25m 충돌(벽 세그먼트, 문 구간 통과, 가구 OBB 에지), Space 시점 1.60/1.15 토글, 진입 시 0.6s 하강 트랜지션
  - 응시 레이캐스트 3m → 가구명·거리 칩, E 편집 패널(스와치 → SSOT 즉시 반영), P 스크린샷
  - HUD(1d 풀 계기판): ESC 칩·다크 뷰탭·크로스헤어·미니맵(위치 점+시야 콘)·WASD 안내·조명 패널(프리셋 3종 0.4s 보간·실내등·시야각 슬라이더)
  - WebGL 불가 환경용 CanvasBoundary 폴백 (앱 크래시 방지)
  - ⚠️ 3D 실렌더는 헤드리스 브라우저가 WebGL 미지원(SwiftShader 실패)이라 시각 검증 못함 — HUD만 검증. 실기기 확인 필요
- [x] 테스트 (Vitest, 43개 통과): geometry(shoelace·point-in-polygon·OBB 코너·SAT 충돌·스냅)·견적 셀렉터(룸별 합계)·store(undo/redo 명령단위·커밋/비커밋·pushHistory·localStorage 2s 디바운스 직렬화·openPlan/addPlan)·snapItemMove(그리드/벽면/여유)·findFreeSpot·wallBoxes/collisionSpans·resolveCollisions(캡슐 0.25m) — `npm test`
  - 테스트가 잡은 실버그: 벽 스냅 clearance 방향 판정의 부동소수 경계 오류 → 중심 기준 판정으로 수정
- [x] ④ 1f 조감도 — 빌드+테스트 통과, 패널 브라우저 검증 (2026-08-24)
  - OrbitControls 조감(돌하우스 퍼스펙티브 / 단면 y=1.2m 클리핑 / 평행 투상 오소그래픽)
  - 조명 시뮬레이션 패널: 프리셋 2×2(오후3시/해질녘/흐린날/밤), 창 방향 슬라이더(태양 방위각 실반영, 남향/동향/서향 라벨), 일조 시간 파생 카드
  - 표시 패널: 천장 숨기기/그림자/치수 라벨(drei Html 룸 라벨) 토글
  - "이 시점에서 워크스루 시작": 카메라 시선-바닥 교점+yaw 캡처 → 워크스루 스폰
- [x] ⑤ 1h 대시보드 + 견적 — 빌드+테스트 통과, 브라우저 검증 (2026-08-24)
  - 상단 내비(내 도면/가구 컬렉션/공유받은 도면/아바타), 도면 카드 3열(미니 평면도 SVG 썸네일·작업중 뱃지·㎡/가구/₩M 메타), dashed 업로드 카드
  - 우측 392px: 견적 요약(총액 30/800 + 룸별 N점·합계 행 — SSOT에서 파생), 공유 카드(링크 복사·"보는 사람도 3D 이동 가능" 토글), 장바구니 CTA
- [x] ⑥ 1g 업로드 트레이싱 — 빌드+테스트 통과, 3단계 플로우 브라우저 검증 (2026-08-24)
  - 스테퍼(업로드/스케일/벽 확인), 드롭존+파일 선택+샘플 도면, 종이 780×560
  - 2단계: 기준선 드래그(주황 3px+틱) + 실제 길이 입력 칩 → px/m 확정 (자동 인식 결과 카드는 목업 수치)
  - 3단계: 수동 트레이싱(그린 오버레이·정점 드래그 수정·시작점 클릭 닫기=룸·Enter 확정), 현황 카드(벽/룸/면적 실시간)
  - "에디터에서 열기": px→m 변환해 Plan 생성(unitScale·tracing 원본 반투명 오버레이 포함) → 에디터 진입. "빈 도면에서 직접 그리기" 지원
  - 샘플 도면 사용 시 자동 인식 결과를 프리필(목업 수준 — README 지시대로 실제 인식 없음)

## 피드백 반영 (2026-08-24, 사용자 3건)

- [x] 1) 가구 확충: 카탈로그 12→22종. 신규 shape 3종(desk/dining-set/plant — 2D 심볼+3D 프리미티브 페어) + 기존 shape 재활용 엔트리(다이닝 체어·사이드 테이블·와이드 책장·TV장 1800·여닫이 옷장·3구 스탠드). '소품' 카테고리 pill 추가(화분 2종). 빌드+테스트 통과, 브라우저 확인.
- [x] 2) 워크스루 충돌 슬라이딩: `moveAndSlide` — 접촉 법선으로 이동 벡터 접선 투영(속도 보존), 코너 2법선 2패스 순차 투영(양쪽 막힘일 때만 정지), 최종 침투 해소로 관통 방지. 가구 유효 반경 0.25→0.18m 완화(FURNITURE_PAD −0.07, 벽은 스펙대로 0.25 유지), h≤0.4m 낮은 오브젝트(러그·커피 테이블) 충돌 제외. 슬라이딩/코너/좁은 통로/패딩 테스트 8개 추가(총 51개). 빌드+테스트 통과.
- [x] 3) 3D 뷰 undo/redo: `useUndoShortcut` 전역 훅(App 마운트)으로 이동 — 2D·워크스루·조감도 전 화면에서 Cmd+Z/Cmd+Shift+Z 동작(포인터 락 중 keydown도 수신, input/textarea 포커스 시 제외). Editor2D의 중복 핸들러 제거(이중 실행 방지). E-편집 소재 변경은 updatePlan commit 경로라 자동으로 명령 단위 히스토리에 쌓임. 브라우저 검증: 2D 삭제 → 3D 뷰에서 Cmd+Z → 복원 확인. 빌드+테스트(51개) 통과.

### 2차 피드백 (2026-08-24, 실기기 확인 후 toy-c7 경유 3건)

- [x] 1) 포인터 락 UX: **Tab**으로 커서 모드 ↔ 이동 모드 토글 (ESC도 기존대로 해제). 커서 모드에선 일반 커서로 HUD 패널 조작 가능. 조작 안내 패널에 TAB 키캡 힌트(상태별 라벨), E-편집 패널 열림 중 재락 방지.
- [x] 2) 사물 상호작용: 락 상태에서 크로스헤어 조준+클릭 → **조명 on/off** (emissive+PointLight 실소등, `PlacedItem.powered` 필드·`togglePower` 순수 모듈). 상호작용 가능 사물 응시 시 와이어프레임 하이라이트 + "클릭 · 켜기/끄기" 칩. updatePlan commit 경로라 undo 대상. 테스트 7개. **문 여닫기·TV 켜기는 후속** (문은 개구부가 벽 모델 소속이라 충돌·메시 분할 연동 필요).
- [x] 3) 가구 추가 확충: 22→**35종** — 침실(싱글 베드·협탁·화장대), 주방(아일랜드 카운터·바 스툴·냉장고), 욕실(수납장·드럼 세탁기), 사무(오피스 체어·파일 캐비닛), 조명(테이블 램프)·러그(샤기)·소품(선인장). 카탈로그 무결성 테스트 4개(id 유일성·카테고리 균형·표기 규칙). 총 테스트 62개.

## 현재 상태

- 프로젝트: `/Users/kyb-ontact/sonix/toy/roomcast` (git 초기화됨, 리모트 없음)
- `npm run build` / `npm test`(62개) **통과** ✅
- 스택: React 18.3 + TS + Vite 8 / three 0.170 + r3f 8 + drei 9 / zustand 5
- SSOT `Plan` 모델·카탈로그·샘플 도면·지오메트리 유틸·스토어(undo/localStorage 저장) 완성
- 화면 셸: 상태 기반 라우팅(dashboard/editor/upload × 2d/walkthrough/birdseye), 상단바·뷰 탭 구현

## Questions (합리적 가정으로 진행 중)

- **React 버전**: Vite 스캐폴드는 React 19였으나 지시대로 React 18.3으로 고정.
  r3f는 v8, drei는 v9 (React 18 호환 최종 메이저), three는 drei 9 peer 범위인 0.170.
- **룸 면적**: 핸드오프의 룸 라벨 값(거실 31.2㎡ 등)은 도면 지오메트리와 불일치
  (합계도 84㎡와 안 맞음). 지오메트리에서 shoelace로 **자동 계산한 실제 값**을 표기함.
- **Room 타입**: README 타입에 `polygon` 필드를 추가(렌더·판정용). ARCHITECTURE.md 참조.
- **상태관리**: README에 라이브러리 지정이 없어 zustand 채택 (r3f 표준 조합).

- **치수 도구**: Plan 타입에 치수 주석 필드가 없어(README 준수) 치수 도구는
  드래그 측정(비영속) + 도면 외곽 자동 치수선으로 구현. 영속 치수 주석은 후속 과제.
- **1c-4 "42cm 겹침" 수치**: 겹침 깊이 계산 대신 겹친 가구명 표기로 대체.

## 로그

- 2026-08-24: 프로젝트 생성, 단계 ① 완료.
- 2026-08-24: 단계 ② 완료 — 2D 에디터 전체 인터랙션 + 브라우저 스모크 테스트(선택·드래그·스냅·충돌 확인).
- 2026-08-24: 단계 ③④⑤⑥ + Vitest 테스트 43개 완료. **전 단계 완료** — `npm run build`·`npm test` 통과.
- 남은 확인: 3D 실렌더(워크스루·조감도)는 WebGL 되는 실기기에서 육안 확인 필요 (헤드리스는 SwiftShader 실패로 폴백 표시됨).
