# Roomcast 진행 상태

다른 세션 모니터링용. 단계 완료 시마다 갱신.

## 체크리스트

- [x] ① 스캐폴드 + 디자인 토큰(CSS 변수) + 상단바/뷰 탭 — 빌드 통과 (2026-08-24)
- [x] ② 1a 2D 에디터 — 빌드 통과 + 헤드리스 브라우저 검증 (2026-08-24)
  - 벽·룸·개구부(문 스윙 호/창)·외곽 치수선·룸 라벨 SVG 렌더
  - 가구 심볼 11종(top-view), 러그<가구<조명 z-순서
  - 카탈로그 패널(카테고리 pill·배치중 뱃지·클릭 배치 고스트), 인스펙터(X/Y/회전/높이 편집·스와치·가격), 툴독, 상태바(축척·스냅 토글·배치율)
  - 인터랙션: 선택/이동(그리드 10cm+벽면 12px 스냅, 정렬선+"벽에 붙임"+여유 라벨), 회전(15° 링, Shift 프리), 코너 리사이즈, 충돌(SAT, 경고 칩+드롭 후 "빈 자리로 이동/그대로 두기"), 벽 그리기(15°/10cm 스냅, 닫으면 룸 생성), 문/창 추가, 치수 측정, 팬(Space/휠클릭)·커서 기준 휠 줌
  - 단축키: V/W/D/N/M, Delete, Cmd+D, Cmd+Z/Shift+Z, 방향키 10cm(Shift 1cm), Esc
  - undo/redo 명령 단위, localStorage 2s 디바운스 자동저장("N분 전 저장")
- [x] ③ 1d 3D 워크스루 — 빌드+테스트 통과, HUD 브라우저 검증 (2026-08-24)
  - Plan→3D 파생: 룸 바닥(Shape)·벽 개구부 분할(문 상인방/창 sill·head·유리)·천장·가구 프리미티브 11종·램프 PointLight
  - PointerLockControls + WASD(1.4/Shift 3.0 m/s), 캡슐 0.25m 충돌(벽 세그먼트, 문 구간 통과, 가구 OBB 에지), Space 시점 1.60/1.15 토글, 진입 시 0.6s 하강 트랜지션
  - 응시 레이캐스트 3m → 가구명·거리 칩, E 편집 패널(스와치 → SSOT 즉시 반영), P 스크린샷
  - HUD(1d 풀 계기판): ESC 칩·다크 뷰탭·크로스헤어·미니맵(위치 점+시야 콘)·WASD 안내·조명 패널(프리셋 3종 0.4s 보간·실내등·시야각 슬라이더)
  - WebGL 불가 환경용 CanvasBoundary 폴백 (앱 크래시 방지)
  - ⚠️ 3D 실렌더는 헤드리스 브라우저가 WebGL 미지원(SwiftShader 실패)이라 시각 검증 못함 — HUD만 검증. 실기기 확인 필요
- [x] 테스트 (Vitest, 43개 통과): geometry(shoelace·point-in-polygon·OBB 코너·SAT 충돌·스냅)·견적 셀렉터(룸별 합계)·store(undo/redo 명령단위·커밋/비커밋·pushHistory·localStorage 2s 디바운스 직렬화·openPlan/addPlan)·snapItemMove(그리드/벽면/여유)·findFreeSpot·wallBoxes/collisionSpans·resolveCollisions(캡슐 0.25m) — `npm test`
  - 테스트가 잡은 실버그: 벽 스냅 clearance 방향 판정의 부동소수 경계 오류 → 중심 기준 판정으로 수정
- [x] ④ 1f 조감도 — 빌드+테스트 통과, 패널 브라우저 검증 (2026-08-24)
  - OrbitControls 조감(돌하우스 퍼스펙티브 / 단면 y=1.2m 클리핑 / 평행 투상 오소그래픽)
  - 조명 시뮬레이션 패널: 프리셋 2×2(오후3시/해질녘/흐린날/밤), 창 방향 슬라이더(태양 방위각 실반영, 남향/동향/서향 라벨), 일조 시간 파생 카드
  - 표시 패널: 천장 숨기기/그림자/치수 라벨(drei Html 룸 라벨) 토글
  - "이 시점에서 워크스루 시작": 카메라 시선-바닥 교점+yaw 캡처 → 워크스루 스폰
- [x] ⑤ 1h 대시보드 + 견적 — 빌드+테스트 통과, 브라우저 검증 (2026-08-24)
  - 상단 내비(내 도면/가구 컬렉션/공유받은 도면/아바타), 도면 카드 3열(미니 평면도 SVG 썸네일·작업중 뱃지·㎡/가구/₩M 메타), dashed 업로드 카드
  - 우측 392px: 견적 요약(총액 30/800 + 룸별 N점·합계 행 — SSOT에서 파생), 공유 카드(링크 복사·"보는 사람도 3D 이동 가능" 토글), 장바구니 CTA
- [x] ⑥ 1g 업로드 트레이싱 — 빌드+테스트 통과, 3단계 플로우 브라우저 검증 (2026-08-24)
  - 스테퍼(업로드/스케일/벽 확인), 드롭존+파일 선택+샘플 도면, 종이 780×560
  - 2단계: 기준선 드래그(주황 3px+틱) + 실제 길이 입력 칩 → px/m 확정 (자동 인식 결과 카드는 목업 수치)
  - 3단계: 수동 트레이싱(그린 오버레이·정점 드래그 수정·시작점 클릭 닫기=룸·Enter 확정), 현황 카드(벽/룸/면적 실시간)
  - "에디터에서 열기": px→m 변환해 Plan 생성(unitScale·tracing 원본 반투명 오버레이 포함) → 에디터 진입. "빈 도면에서 직접 그리기" 지원
  - 샘플 도면 사용 시 자동 인식 결과를 프리필(목업 수준 — README 지시대로 실제 인식 없음)

## 피드백 반영 (2026-08-24, 사용자 3건)

- [x] 1) 가구 확충: 카탈로그 12→22종. 신규 shape 3종(desk/dining-set/plant — 2D 심볼+3D 프리미티브 페어) + 기존 shape 재활용 엔트리(다이닝 체어·사이드 테이블·와이드 책장·TV장 1800·여닫이 옷장·3구 스탠드). '소품' 카테고리 pill 추가(화분 2종). 빌드+테스트 통과, 브라우저 확인.
- [x] 2) 워크스루 충돌 슬라이딩: `moveAndSlide` — 접촉 법선으로 이동 벡터 접선 투영(속도 보존), 코너 2법선 2패스 순차 투영(양쪽 막힘일 때만 정지), 최종 침투 해소로 관통 방지. 가구 유효 반경 0.25→0.18m 완화(FURNITURE_PAD −0.07, 벽은 스펙대로 0.25 유지), h≤0.4m 낮은 오브젝트(러그·커피 테이블) 충돌 제외. 슬라이딩/코너/좁은 통로/패딩 테스트 8개 추가(총 51개). 빌드+테스트 통과.
- [x] 3) 3D 뷰 undo/redo: `useUndoShortcut` 전역 훅(App 마운트)으로 이동 — 2D·워크스루·조감도 전 화면에서 Cmd+Z/Cmd+Shift+Z 동작(포인터 락 중 keydown도 수신, input/textarea 포커스 시 제외). Editor2D의 중복 핸들러 제거(이중 실행 방지). E-편집 소재 변경은 updatePlan commit 경로라 자동으로 명령 단위 히스토리에 쌓임. 브라우저 검증: 2D 삭제 → 3D 뷰에서 Cmd+Z → 복원 확인. 빌드+테스트(51개) 통과.

### 2차 피드백 (2026-08-24, 실기기 확인 후 toy-c7 경유 3건)

- [x] 1) 포인터 락 UX: **Tab**으로 커서 모드 ↔ 이동 모드 토글 (ESC도 기존대로 해제). 커서 모드에선 일반 커서로 HUD 패널 조작 가능. 조작 안내 패널에 TAB 키캡 힌트(상태별 라벨), E-편집 패널 열림 중 재락 방지.
- [x] 2) 사물 상호작용: 락 상태에서 크로스헤어 조준+클릭 → **조명 on/off** (emissive+PointLight 실소등, `PlacedItem.powered` 필드·`togglePower` 순수 모듈). 상호작용 가능 사물 응시 시 와이어프레임 하이라이트 + "클릭 · 켜기/끄기" 칩. updatePlan commit 경로라 undo 대상. 테스트 7개. **문 여닫기·TV 켜기는 후속** (문은 개구부가 벽 모델 소속이라 충돌·메시 분할 연동 필요).
- [x] 3) 가구 추가 확충: 22→**35종** — 침실(싱글 베드·협탁·화장대), 주방(아일랜드 카운터·바 스툴·냉장고), 욕실(수납장·드럼 세탁기), 사무(오피스 체어·파일 캐비닛), 조명(테이블 램프)·러그(샤기)·소품(선인장). 카탈로그 무결성 테스트 4개(id 유일성·카테고리 균형·표기 규칙). 총 테스트 62개.

### 3차 지시 (2026-08-24, 실기기 승인 후 후속 2건)

- [x] 1) 문 여닫기: `Opening.open` 필드(undefined=열림) + `toggleDoor` 순수 모듈. 워크스루에서 문짝 조준+클릭 → 개폐 토글, **충돌 연동**(닫힌 문 = 벽처럼 차단, 열린 문 = 통과 — collisionSpans가 open 상태 반영). 3D 문짝 메시(경첩·스윙 방향 반영, 95° 회전 스윙 애니메이션 ~0.4s, 손잡이) + 응시 하이라이트 + "클릭 · 열기/닫기" 칩. 2D는 닫힘=슬래브/열림=스윙 호로 구분 렌더. 샘플 도면 욕실 문 기본 닫힘(데모 겸 충돌 케이스). undo 대상.
- [x] 2) TV 켜기: 'tv' shape 신설 — 스탠드형 TV 카탈로그 추가(2D 심볼+3D 메시: 베이스·폴·스크린). 조명과 동일 패턴(조준+클릭, powered/togglePower, 하이라이트+칩, undo). 켜면 화면 emissive(#dfe8ff) + 약한 화면광 PointLight. 샘플 거실에 배치(i-tv).
- 테스트: 문 개폐 토글/기본값/no-op, 닫힌 문 충돌 유지·열면 통과(단위+샘플 도면 통합), TV 상호작용 5건 등 8개 추가 — **총 72개 통과**.

### 4차 지시 (2026-08-24, "평형별 인테리어 템플릿")

- [x] 평형 템플릿 3종 (`src/model/templates.ts`): **원룸 스튜디오**(전용 23㎡, 욕실 분리)
  / **25평 아파트**(전용 59.4㎡ — 안방·침실·욕실·주방·거실, 개방 통로 divider)
  / **34평 아파트**(전용 84.2㎡ — 침실2·침실3(서재)·욕실·안방·주방·거실, 국민평형).
  각각 벽·문(욕실 기본 닫힘)·창 + 카탈로그 기본 가구 배치(8~23개) 완비.
- [x] UI: 업로드 1단계 우측 패널 "또는 템플릿에서 시작" — MiniPlan 썸네일 카드 3종 + "빈 도면에서 시작". 클릭 → 즉시 새 plan 생성·에디터 진입. MiniPlan을 공용 컴포넌트로 추출(대시보드와 공유), plan 빌더 헬퍼 `planBuilder.ts` 분리(samplePlan 리팩터).
- 테스트 19개 추가(총 91개): 면적 평형 근사(±1.5㎡)·id 유일성·개구부 벽 적합·가구 카탈로그/룸 내부 배치·**배치 가구 상호 충돌 없음**·build() 인스턴스 독립성.

### 5차 지시 (2026-08-24, "템플릿 실평면 보정" — toy-c7 검증 기반)

- [x] 욕실 현실화: **34평 욕실 2개** — 공용 4.0㎡ + 안방 부속 3.6㎡(안방에서만 진입, w-k-b 벽 문). **25평도 욕실 2개** — 공용 3.6㎡ + 안방 부속 2.4㎡(샤워형). 기본 닫힘 문은 템플릿마다 공용/욕실 문 1개씩 유지(데모).
- [x] 안목치수 전환: 룸 폴리곤을 벽 중심선 → **내벽면(0.075 인셋)** 기준으로 전면 재작성. 외곽 보정(원룸 6.2×4.0 / 25평 9.3×7.0 / 34평 10.9×8.4) 후 순면적 합 = 22.8 / 59.0 / 83.3㎡ (표기 대비 ±1.5 이내).
- [x] 평면 디테일: 현관 분리(25평 2.8㎡·34평 4.3㎡, 신발장·러너), 34평 주방 팬트리 1.9㎡(선반+문), 남측 전면창 유지(거실 2.6~3.0m), 침실 배분 안방 12.5 > 침실2 10.9 > 침실3(서재) 7.8. 34평은 4인 식탁을 LDK 거실 측에 배치.
- 테스트 8개 추가(총 99개): 욕실 개수/면적 범위, 안방욕실 문 존재, 기본 닫힘 문, 현관/팬트리 존재, 침실 배분 순서, 안목 인셋 검증. 기존 무결성 테스트(충돌 없음·룸 내부 배치)도 신규 지오메트리로 전부 통과.

### 6차 지시 (2026-08-24, "문서 오픈 시 fit-to-view")

- [x] `fitCamera(plan, viewport, padding=80px)` 순수 함수 — 도면 bounds+여백이 뷰포트에 맞는 zoom 계산(0.3~2.0 클램프, pan=0 중앙정렬). 스토어 `pendingFitView` 플래그: openPlan(대시보드 열기·템플릿/빈 도면 생성 직후 공통 경로)·첫 로드 시 set → Editor2D가 뷰포트 실측 후 1회 소비. 열려 있는 동안의 사용자 팬/줌은 불변. "뷰 리셋" 버튼도 동일 fit 로직으로 통일. 회귀 테스트 3개(bounds 포함·거대 도면 축소·소형 도면 확대 상한) — 총 102개. 브라우저 확인: 4×3m 서재 문서 오픈 시 화면 가득(1:30).

### 7차 지시 (2026-08-24, "문 여닫기 이어서 + 문 앞 가구 배치 금지")

- [x] **2D 문 클릭 토글**: 선택 도구로 문 글리프 클릭 → 여닫기 (호버 시 pointer 커서). 3D 워크스루 토글(3차 구현)과 같은 `toggleDoor` SSOT 경로 — undo 대상, 2D 슬래브/호 표현·3D 스윙·충돌에 즉시 반영.
- [x] **문 클리어런스 존** (`src/model/doorZones.ts`): 문마다 스윙 존(문 폭 × 문 폭, 문짝 회전 반경) + 통행 스트립(반대편 0.3m). 개폐 상태 무관 적용, 러그·펜던트 제외.
  - 에디터: 드래그·배치 고스트가 존 침범 시 주황 존 오버레이 + "문 앞 공간을 막음" 경고 칩 + warn 외곽선(비파괴 — 배치는 허용). 드롭 후 "빈 자리로 이동" 액션이 문 존도 회피.
  - 샘플 2종 + 템플릿 3종 **전수 보장**: 모든 가구가 어떤 문 존도 침범하지 않도록 배치·스윙 방향 보정(현관문 밖여닫이, 침실 문 안여닫이, 욕실/팬트리 문은 내부로). 테스트가 위반 자동 검출.
- 테스트 12개 추가(총 114개): 존 지오메트리(스윙 방향별·통행 깊이)·blockedDoorIds(스윙/통행/무시 shape)·샘플+템플릿 전수 규칙. 브라우저 검증: 2D 문 클릭 토글(슬래브↔호), 소파 드래그 시 존 오버레이+경고 칩.

### 8차 지시 (2026-08-24, "소품 확충")

- [x] 소품 4→13종 (카탈로그 36→45): 스탠드형 대형 화분·전신 거울·빨래 건조대·캣타워·공기청정기·스탠드 선풍기·휴지통·장난감 바구니·**무드등**(floor-lamp shape 재활용 → 클릭 전원 토글·PointLight 자동 배선). 신규 shape 5종(mirror/fan/bin/rack/cat-tower) 2D 심볼+3D 프리미티브 페어, 현실 치수. h≤0.4 소품(휴지통·바구니·무드등)은 워크스루 충돌 자동 제외.
- **스킵 사유**: 벽 소품(액자·벽시계·벽거울)과 테이블 위 소품(꽃병·책·노트북)은 현재 모델이 바닥 배치 전용(벽 부착 좌표계·표면 적층 없음)이라 제외. **벽 부착 시스템·표면 스태킹은 별도 기능 후보**로 남김.
- 테스트: 카탈로그 45종·decor 12종 이상 검증, 무드등 상호작용 배선 (총 116개).

### 9차 지시 (2026-08-24, 승인 기능 4건)

- [x] 1) 영속 치수 주석: `Plan.dimensions?: DimensionNote[]` (README 원 타입에 없는 실용적 추가 — optional이라 기존 저장본 호환). 치수 도구(M) 드래그 측정 → pointerup 시 도면에 고정(0.05m 미만은 무시). 선택 도구로 치수선 클릭 → 선택(accent 강조), Delete 삭제, 추가/삭제 모두 명령 단위 undo. 렌더는 틱 치수선+길이 칩(2D — 3D는 선택 사항이라 스킵). 테스트 4개(hit-test·optional 호환·추가/삭제 undo) — 총 120개.
- [x] 2) 바닥/벽 마감재: `finishes.ts` — 바닥 8종(원목 오크/월넛·헤링본·타일 2종·마블·장판·콘크리트, 2D/3D 색 페어) + 벽지 7색. `Room.floorFinish/wallFinish` optional 필드(저장본 호환). 선택 도구로 룸 빈 바닥 클릭 → 룸 선택(틴트+대시 아웃라인) → 인스펙터 자리에 마감 패널(바닥 리스트+벽지 스와치, 재클릭=기본 복원). 2D 룸 채움·3D 바닥 즉시 동기, 3D 벽은 세그먼트 중점이 접한 룸의 벽지 적용(`resolveWallColor` — 내벽 양쪽 다른 색이면 첫 샘플 우선, 면 분리 렌더는 후속 한계로 기록). undo 대상. **견적 반영은 '선택' 항목이라 스킵.** 테스트 7개(팔레트 무결성·fallback·patch·벽색 해석) — 총 127개.
- [x] 3) 도면 JSON 내보내기/가져오기: `planIO.ts` — `{format:'roomcast-plan', version:1, exportedAt, plan}` 포맷. 대시보드 '백업' 카드에서 현재 도면 .json 다운로드 / 파일 선택 가져오기(검증 통과 시 **새 문서로 추가** + 에디터 진입, id 재발급). 스키마 검증: format/version(미래 버전 거부)/벽·개구부(t 범위, 벽 참조 존재)·룸·가구 필드 — 실패 시 한국어 에러를 카드에 표시. **round-trip 테스트**: 샘플+템플릿 전부 id 외 완전 보존 + 확장 필드(dimensions·마감재·powered·문 open) 보존 검증. 테스트 10개 — 총 146개.
- [x] 4) 다중 선택·복제: **Shift+클릭** 토글, **Shift+드래그** 마퀴 박스(중심 포함 기준, accent 틴트 렌더). 다중 선택 상태에서 선택 아이템을 잡으면 **그룹 이동**(시작 위치 기준 delta 재계산 — 누적 오차 없음, 그리드 스냅, 그룹 멤버끼리 충돌 제외·외부 충돌/문 존 union 경고, roomId 재배정, 명령 단위 undo). Cmd+D 그룹 복제·Delete 일괄 삭제는 기존 selection 배열 로직이 그대로 커버. 테스트 9개(itemsInRect·translateItems·groupProblems) — 총 136개.

### 11차 지시 (2026-08-24, 보완 승인 4건)

- [x] 2) 미닫이문: `Opening.doorType?: 'hinged'|'sliding'`(undefined=여닫이). 2D=레일 2줄+슬래브(열림=파킹측 절반), 3D=슬라이드 애니메이션+상단 레일(회전 없음), 클릭 토글·충돌 연동은 여닫이와 동일 SSOT(toggleDoor). doorZones: 미닫이는 스윙 존 없이 **양쪽 통행 스트립 0.3m**만. planIO doorType 검증. 템플릿 적용: **84평 팬트리 문 + 25평 안방욕실 문을 미닫이로**(거실 남측은 발코니 없는 전면창이라 유지 — 판단 사유). 워크스루 칩 '미닫이문' 표기. 테스트 2개 — 총 152개.
- [x] 1) 벽 부착 시스템 + 벽 소품: `Plan.wallItems?: WallItem[]` — 벽 세그먼트 좌표계(wallId + t 0..1 + heightM + side front/back). 소품 4종(액자 S/L·벽시계·벽거울, 소품 카테고리). 배치 UX: 카탈로그 클릭 → 40px 이내 벽에 스냅 고스트(유효/무효 색) → 클릭 배치, 선택 후 드래그로 벽 간 이동(무효 드롭은 원위치 복원). **개구부·기존 벽 아이템과 겹침은 하드 금지**, doorZones/가구 충돌은 비적용(스펙대로). 2D 플랜 뷰 글리프+카탈로그 정면 뷰 썸네일, 3D 벽면 부착 메시(액자 캔버스·시곗바늘·거울 반사면). 인스펙터(부착 높이·스와치·삭제), Delete·undo 대상. 견적: totalPrice/priceByRoom '기타(벽)' 그룹 합산. planIO 검증+round-trip. 테스트 10개 — 총 162개.
- [x] 3) 마감재 마무리: ⓐ`wallFaceColors` — 내벽 양쪽 룸 벽지가 다르면 3D 벽에 면별 패널(±법선 면) 분리 렌더(STATUS 한계 해소). ⓑ견적 반영 — 마감 단가 추가(바닥 ㎡당 35k~150k·벽지 12k~16k), `finishCost` = 바닥 단가×룸 면적 + 벽지 단가×(둘레×2.4m, 개구부 차감 없는 근사). 대시보드 견적 카드에 '마감 시공' 섹션+총액 합산. 테스트 4개.
- [x] 4) 주간/야간 토글: `toggleDayNight`(야간↔오후, 타 프리셋에서는 야간으로). 워크스루 좌상단 칩·조감도 카메라 바에 토글 배치. 야간 프리셋은 태양 0.12·앰비언트 0.22로 낮고 실내등 배율 2.2라 켜진 조명·무드등·TV 화면광이 주광원이 됨(설계 가드 테스트로 고정). 뷰 설정이라 plan 저장 안 함. 테스트 2개 — 총 168개.

### 14차 (2026-08-25, "셀프 도그푸딩 — 빈 도면에서 샘플 수준 재현")

빈 도면 → 벽 그리기(외곽 폐곡선+내벽) → 문/창 → 미닫이 전환 → 가구 → 방별 마감 → 치수 주석 → undo/redo 전수 → 견적 → JSON 내보내기 → 3D 탭 전체 워크플로우를 헤드리스 브라우저로 실사용 재현. 발견·처치:

| # | 분류 | 발견 | 처치 |
|---|---|---|---|
| D1 | 누락 | 내벽을 그려도 룸이 분할되지 않음 — 라벨·면적·마감이 통짜 룸 기준 | `roomSplit.ts` 신설: 열린 폴리라인 양끝이 룸 경계(±0.15m)면 폴리곤 분할. 면적 보존 검증(±5%), 이름·용도·마감 상속, 가구 roomId 재배정. ㄱ자 절단 지원 |
| D2 | 버그 | 벽 그리기 길이 스냅(10cm)이 끝점을 기존 벽에서 ~0.2m 못 미치게 잘라 분할 실패·접합 불량 | 벽 그리기에 **기존 벽 스냅** 추가 — 벽 끝점 우선(18px), 벽 선상 최근접점(18px) |
| D3 | 마찰 | 같은 가구 연속 배치 불가(배치마다 카탈로그 재클릭) | 연속 배치 유지 — Esc/카탈로그 재클릭으로 종료. 배치 직후 자동 선택·충돌 배너는 제거(고스트 색으로 대체) |
| D4 | 마찰 | 카탈로그·인스펙터 패널 겹침(마감 패널이 김) | 인스펙터 max-height 44vh + 내부 스크롤 |
| D5 | 마찰 | 룸 선택 틴트(6%)가 마감색 미리보기 왜곡 | 3%로 완화 |
| D6 | 부수 해소 | 룸 라벨이 내벽·문과 겹침 | D1 분할 후 각 방 중심으로 이동 |

- **undo/redo 전수 점검**: 9단계 조작(폐곡선·내벽·문·창·가구 2·미닫이 전환·마감·치수) undo×9 → 완전 빈 도면, redo×9 → 완전 복원 확인. 드래그류 1엔트리(pushHistory)·코얼레싱(13차)·저장 2s 디바운스·히스토리 상한 100 확인.
- **재검증(2회차)**: 같은 흐름 재실행 — 룸 분할(방1 22.0/방2 23.2㎡)·방별 마감(그레이 타일 우측만)·연속 배치(체어 3연타)·견적(가구+마감 합산)·JSON 내보내기(콘솔 에러 0) 클린 완주.
- **보류(스펙 수준)**: 기존 벽 여러 개 조합의 닫힌 공간 자동 인식(일반 topology), T자 접합 정점 고정, 템플릿(안목치수 폴리곤) 룸의 벽 편집 동기.
- 테스트 5개 추가(roomSplit) — 총 185개.
- [x] **후속(toy-c7 검증 발견)**: 분할 벽 오버슛 시 분할 실패 → `trimPolylineToRooms` — 폴리라인 끝점이 룸 경계를 관통하면 교차점에서 트리밍(양끝 완전 관통 직선 포함), 트리밍된 좌표로 벽 생성(돌출 벽토막 방지) 후 분할 인정. 테스트 3개 — 총 188개. 브라우저 검증: 40px 관통 그리기 → 정상 분할.

### 16차 (2026-08-25, 2순위 ⓐⓑ)

- [x] ⓐ 도면 PNG/인쇄(PDF) 내보내기: `ExportSvg.tsx` — PlanCanvas 렌더 조각(벽·개구부·룸 라벨·외곽 치수·치수 주석·벽 부착·마감 채움) 재사용한 정적 SVG(90px/m, 여백 포함, CSS 변수 폰트 실스택 치환, 푸터에 도면명·면적). 대시보드 '내보내기·백업' 카드: **PNG 저장**(SVG→canvas 2x 래스터), **인쇄(PDF)**(새 창 + @page 인쇄 CSS + window.print — 브라우저 'PDF로 저장' 이용, 외부 의존 0). 테스트 4개(독립 SVG·폰트 치환·확장 요소 포함·템플릿 전수 렌더).
- [x] ⓑ 가구 수치 입력 검증: 인스펙터 X/Y/회전/높이 입력에 반응형 검증 행 — 겹침 N건·문 앞 침범 N곳 경고(비파괴, "배치는 유지됩니다"). **판단**: 정밀 입력 목적이라 그리드 스냅은 타이핑 값에 미적용(드래그와 다른 점 — 명시), 경고 정책은 드래그와 동일. 브라우저 검증: 소파를 침대 좌표로 수치 이동 → 경고 표시.
- 테스트 4개 — 총 192개.

### 17차 (2026-08-25, "상품 연동 — 실판매가·실측 제원·상품 링크")

- [x] 데이터 계층: `ProductInfo` 타입(url/priceKrw/mall/brand?/specW·D·H?/source?/fetchedAt) + `src/model/products.json`(카탈로그 id 키). `applyProducts` 머지 — 상품이 있으면 카탈로그 price→실판매가, size→실측 제원(spec 없는 축은 기존 유지)으로 대체 후 `product` 필드 부착. 모듈 로드 시 카탈로그에 일괄 적용(신규 배치부터 반영, 기존 배치 스냅샷은 불변).
- [x] 초기 큐레이션 8건 — **헤드리스 브라우저로 실제 상품 페이지를 열어 확인한 값만 입력**(추측·임의 URL 금지 규칙): KIVIK 3인 소파·LACK 커피테이블·BILLY 책장·SLATTUM 퀸 침대·MALM 책상·TEODORES 의자·BESTÅ TV장 180·KLEPPSTAD 옷장 (IKEA 한국, 가격+치수 아코디언의 폭/깊이/높이 실측). SLATTUM 은 페이지 표기 높이(40cm)가 프레임 높이라 전고와 의미가 달라 specH 생략.
- [x] 동기화 스크립트 소스 중립화: 네이버 쇼핑 검색 API가 2026-07-31 종료되어 `scripts/sync-products.mjs`를 **어댑터 구조**로 전환 — 기본 실행은 products.json 스키마 검증(https·양수 가격·spec 0~5m·카탈로그 id 존재), `--source X`는 ADAPTERS 레지스트리 조회(현재 라이브 어댑터 없음). 어댑터 결과는 큐레이션 spec·source 를 덮어쓰지 못함(mergeProduct). 향후 쿠팡 파트너스 등 새 소스는 어댑터만 추가하면 됨.
- [x] UI: 카탈로그 카드에 실판매가+↗ 상품 링크, 인스펙터 '실제가' 뱃지+링크, 대시보드 견적 캡션 "실판매가 N점 · 추정가 M점 기준 — 가격은 조회 시점 기준".
- [x] 실측 파급 반영: LACK 높이 0.45m가 걸어넘기 임계(0.4m)를 넘어 커피 테이블이 3D 충돌체가 됨(테스트를 실측 기준으로 갱신), 스튜디오 템플릿 책상(MALM 1.51m)이 현관문 클리어런스를 침범해 배치 이동(x 2.6→2.4).
- 테스트 10개(머지·검증·머지 보존·실데이터 무결성) — 총 200개. build·검증 스크립트 통과.

### 18차 (2026-08-25, "실상품 기반 카탈로그 확충 — 49→73종")

- [x] 신규 24종 추가 — 전 품목 **헤드리스 브라우저로 IKEA 한국 실제 상품 페이지를 열어 가격·치수(치수 아코디언의 폭/깊이/높이)를 확인 후 입력**: 침실(2층 침대 MYDAL·수납 데이베드 BRIMNES·원목 퀸 SONGESAND·화장대 MALM), 아이방(유아용 침대 SUNDVIK·수납 콤비 TROFAST·어린이 테이블 FLISAT), 책상(컴팩트 MICKE·입식 겸용 TROTTEN), 식탁(4인 LISABO·확장형 NORDVIKEN), 체어(암체어 POÄNG·윙체어 STRANDMON), 소파베드(코너 FRIHETEN), 수납(도어 옷장 PAX·서랍유닛 ALEX·트롤리 RÅSKOG·큐브 선반 KALLAX·신발장 STÄLL·행거 RIGGA·사이드보드/미닫이장 VIHALS), 트레이 테이블 GLADOM, 평직 러그 TIPHEDE. 기존 4종 상품 매핑(플로어 HEKTAR·펜던트 NYMÅNE·탁상 FADO·바스툴 DALFRED). products.json 총 36건 — 전부 스키마 검증 통과.
- [x] 카테고리 재편: '침실'(bed)·'아이방'(kids) 신설(8종) — 침대류·협탁·서랍장·장난감 바구니 이동. shape 는 기존 프리미티브 재사용(신규 shape 없음).
- [x] 카탈로그 검색: 패널 상단 이름/브랜드 검색 입력(검색 중 카테고리 무시, 빈 결과 안내). 입력 라우팅 규칙 준수(float-panel 내부 — OVERLAY_SELECTOR 기존 커버, 키 입력은 INPUT 가드 기존 적용).
- [x] 실측 파급: 바스툴 0.35→0.5m로 25평 템플릿 스툴 2개 재배치(테이블 좌·상단), 기존 dresser 이름 '화장대'→'서랍장'(신규 MALM 화장대와 중복 해소).
- 브라우저 검증: 8개 pill·73종 카운트·'침대'/'ikea' 검색(36건)·아이방 아이템 배치. 테스트 200개·빌드 통과.

### 19차 (2026-08-25, "표면 적층 — 테이블 위 소형 아이템 트리형 부착")

- [x] 모델(`surfaces.ts`): `PlacedItem.parentId`(월드 좌표 유지 계약 — 부모 변환 시 헬퍼가 자식 동반), `CatalogItem.mountable`, `SURFACE_SHAPES`(desk/rect·round-table/console/dining-set). 깊이 1단 제한(자식 위 적층 금지). **부모 삭제 시 자식 동반 삭제**(undo 1회로 함께 복원 — 판단 근거: 휴지통 의미론과 일치·예측 가능). 헬퍼: surfaceAt/childFitsSurface/siblingOverlapIds/move·rotateItemWithChildren/deleteItemsWithChildren/mount·unmountItem/mountBaseHeight.
- [x] 배치 UX: mountable 아이템(테이블 램프·무드등·미니 선인장, 이후 모니터 등)을 표면 가구 위로 드래그하면 표면 하이라이트+"○○ 위에 올려놓기" 칩, 밖으로 드래그하면 바닥 배치 전환. 상판 이탈·형제 겹침은 드롭 시 원위치 복원(벽 부착과 동일 정책). 카탈로그 배치 고스트도 표면 기준 검증. 그룹 이동·방향키 이동·수치 입력(X/Y/회전) 모두 자식 동반. 복제는 상판에 안 들어가면 바닥 전환.
- [x] 판정 제외: 표면 자식은 바닥 SAT 충돌·문 클리어런스·3D 워크스루 충돌체에서 제외 — 형제 겹침(표면 로컬)만 검사.
- [x] 2D z-레이어 최상단(레이어 3, 클릭도 자식 우선), 3D 는 부모 상판 높이(baseY)에 렌더 — powered(램프 등) 기존 전원 토글 그대로. 인스펙터: "○○ 위에 올려짐" 배지 + '바닥에 내려놓기'(부모 옆 빈 자리), 부모 삭제 버튼에 "(+올려진 N)" 표기. planIO: parentId 존재·1단 검증 + round-trip.
- [x] 버그 수정(검증 중 발견): 같은 프레임에 pointermove→pointerup 이 몰리면 `planRef`(렌더 시점 갱신)가 스테일 → 마운트 판정이 1제스처 지연. pointerup 판정을 스토어 최신 상태로 변경.
- 브라우저 검증: 책상 배치→램프 올리기(parentId 확인)→밖으로 드래그(해제)→다시 올리기→책상 이동 시 램프 동반(delta 동일) 전 시나리오 통과. 테스트 10개(surfaces) — 총 210개.
- 한계(보류): 부모 리사이즈로 상판이 줄면 자식이 걸쳐도 유지(시각만), 자식 자체 회전으로 상판을 벗어나는 경우 미검증 — 실사용 마찰 시 후속.

### 20차 (2026-08-25, "가전·컴퓨터 확충 — 카탈로그 73→83종, 상품 48건")

- [x] 신규 10종 + 기존 2종(냉장고·세탁기) 매핑 — 전 품목 **다나와 상품 페이지를 직접 열어 대표가(최저가 표기)·크기(가로×세로×깊이 mm)를 확인 후 입력**(toy-c7 세션이 1차 수집, 본 세션이 대표가·치수 전건 재검증): 의류관리기(LG 스타일러 SC5MSR40S)·김치냉장고(LG K322SC)·스탠드 에어컨(삼성 Q9000)·벽걸이 에어컨(삼성 AR07B9350MAS)·식기세척기(LG DUE1BGLE)·전자레인지(삼성 MS23C3535AK)·모니터(LG 울트라기어 27G610A)·노트북(LG 그램 프로16)·데스크톱(삼성 데스크탑5)·모션 스탠딩 책상(카멜마운트 핏쳐 E2 1600). 냉장고=LG 디오스 T876MQQ1H1, 세탁기=LG 워시타워 W2320WANQR(치수는 상세 스펙에서 확인). 페이지에 제원이 없는 노트북·데스크톱은 spec 생략(가격·URL만), 높이 가변(모니터 스탠드·스탠딩 책상)은 specH 생략 — 추측 금지 규칙 유지. mall 표기 '다나와 최저가'.
- [x] 카테고리 '가전'(appliance) 신설 — 기존 냉장고·세탁기·공기청정기·선풍기 이동, 총 9 pill.
- [x] 신규 shape `wall-ac`: 벽걸이 에어컨을 **벽 부착 시스템**으로 배치(WALL_SHAPES 등록, 기본 부착 높이 2.05m, 2D 정면 글리프·3D 본체+송풍구 메시). 전자레인지·모니터·노트북·데스크톱은 `mountable` — 19차 표면 적층으로 책상·수납장 위에 올릴 수 있음.
- 브라우저 검증: 가전 pill·실판매가 표시, 모니터→어린이 테이블 적층(parentId 확인, 상판 이탈 시 배치 거부 동작 확인), 벽걸이 에어컨 벽 배치(창 겹침 구간은 거부됨 확인). 테스트 210개·빌드·products.json 검증(48건) 통과.
- 잔여: 3D 렌더(벽걸이 에어컨 메시·표면 적층 높이)는 실기기 육안 확인 항목.

### 21차 (2026-08-25, "검색 별칭" — toy-c7 검증 후 개선 메모 반영)

- [x] `CatalogItem.keywords`(검색 별칭) + `searchCatalog` 순수 함수 분리(이름·브랜드·별칭 매칭) — "스타일러"→의류관리기, "벙커"→2층 침대, "쇼파"→소파류 등 15개 아이템에 통칭 별칭 부여. 테스트 2개 — 총 212개. 브라우저 검증: '스타일러' 검색 → 의류관리기 1건.

### 22차 (2026-08-25, "Hand(그랩 이동) 도구 + 단축키 확정")

- [x] Hand 도구: 툴바 두 번째(손바닥 아이콘), 지속 팬 모드 — 가구·벽 위 포함 캔버스 어디서나 드래그=팬(선택·배치·이동 없음), 커서 grab/grabbing. Space 홀드·휠클릭 임시 팬은 그대로(차이: Hand는 지속 모드). Esc 또는 V로 선택 복귀. Hand 전환 시 진행 중 카탈로그 배치 자동 취소(기존 setTool 정책). 3D 탭 무관.
- [x] 단축키 확정(사용자 지시): **V=선택, H=핸드, S=선 그리기(스케치 프라이머리, W는 하위호환 별칭)**, D/N/M 유지. IME 조합 대비 물리 키코드(e.code) 기준 매핑(`toolForKeyCode` 순수 함수 분리), 수정키 조합(Cmd+S 등)은 도구 전환 제외. 툴팁 "이동 (H)"/"선 그리기 (S)" 동기화.
- 테스트 3개(도구 상태·팬 델타 수식·키 매핑) — 총 215개. 브라우저 검증: H→소파 위 드래그 시 캔버스만 (80,50) 팬·선택 없음, S/V 전환, 툴팁.

### 23차 (2026-08-25, "선 그리기 — 선 단위 undo (프로그레시브 커밋)")

- [x] A안 채택: 폴리라인 일괄 커밋 → **세그먼트별 즉시 커밋**으로 전환. 두 번째 클릭부터 선 하나 = undo 엔트리 하나(코얼레싱 미적용). 드래프트는 앵커+커밋된 벽 id만 유지하고 미리보기 점열은 plan 파생(`sketchDraft.ts`) — **그리는 중 Cmd+Z 로 마지막 선만 제거되고 그 지점부터 이어그리기**, redo 로 한 선씩 복원.
- [x] 닫기(시작점 클릭) = 마지막 세그먼트+룸 생성이 **별도 1 엔트리**(undo 시 룸·닫는 선만 되돌아가고 그린 선들은 유지). 열린 폴리라인 마감 = 오버슛 트리밍(첫·끝 세그먼트 좌표 보정)+룸 분할 1 엔트리 — 룸 인식·분할 판정은 여전히 finish 시점에만, 변화 없으면 엔트리 미생성. Esc 는 미커밋 앵커·커서만 취소(커밋된 선은 undo 로).
- [x] 파생 회귀 수정: 세그먼트 커밋으로 도면 경계가 자라면 베이스 변환(fit 기준)이 변해 **그리는 중 화면이 밀리는 문제** — 드래프트 시작 시점 plan 으로 변환 기준 고정(팬·줌은 정상 동작, 마감 시 1회 재계산).
- 테스트 2개(파생 점열·스토어 선 단위 undo/redo) — 총 217개. 브라우저 검증: 3선 그리기→undo×1→2선+이어그리기(undo 지점에서 시작 확인), 사각형 닫기→"방 5" 생성·좌표 무드리프트→undo×1 룸+닫는 선만 제거→undo×4 전부 제거.

### 24차 (2026-08-25, "미리보기=커밋 계약 — 드래그 편집 프리뷰")

- [x] 계약 성립 방식: 프리뷰를 별도 경로로 만들지 않고 **드래그 중 라이브 상태(스냅·클램프·거부 반영 완료)가 곧 프리뷰** — 드롭 시 그 상태가 그대로 커밋되므로 프리뷰≠커밋이 구조적으로 불가능. ARCHITECTURE.md에 '미리보기=커밋 계약' 절로 문서화(입력 라우팅 규칙 옆).
- [x] 원본 잔상: 제스처 시작 스냅샷 포즈를 반투명 심볼+점선 외곽으로 원위치에 렌더(`dragPreview.ts`/`DragOriginGhost`) — 단일/그룹 이동·회전·리사이즈(부모는 표면 적층 자식 포함), 벽 끝점/몸통 드래그(원래 벽 선 점선). 유효/무효 색·경고 칩·표면 하이라이트는 기존 DragOverlay 체계 그대로.
- [x] 회전 HUD: 회전 핸들 드래그 중 스냅 적용 라이브 각도 칩(예: "60°").
- [x] Esc = 진행 중 제스처 취소(시작 스냅샷으로 원위치 복원) — move/groupMove/rotate/resize/벽 편집/벽 부착 이동 전부.
- [x] **판단**: 벽 드래그의 룸 면적 라이브 갱신은 미표시 — 드래그 중 경량 판정 유지, 파생 재계산은 커밋 시 1회(성능 원칙, 문서화).
- 테스트 3개(잔상 포즈 파생 — 자식 포함·그룹·벽) — 총 220개. 브라우저 검증: 소파 드래그 중 원위치 잔상+겹침 경고 프리뷰, Esc 원위치 복원(X/Y 원값), 회전 핸들 60° HUD+잔상.

### 25차 (2026-08-25, "정렬 가이드 스냅 + Shift 축 잠금" — pig calculateAlignmentGuides 참조)

- [x] `alignGuides.ts`(순수): 드래그 바운즈의 정렬 포인트(left/centerX/right·top/centerY/bottom)를 대상들의 동일 포인트와 비교 — 임계(6px) 이내면 그 좌표로 스냅하고 두 객체를 잇는 가이드 라인(같은 정렬선의 다중 대상은 전체 구간) 반환. **데드존**(임계~12px): 스냅은 아니지만 그리드를 억제해 정렬선 바로 옆 미세 배치 허용(pig 데드존 개념 적응).
- [x] 스냅 우선순위 재정리(`snapItemMove`): **벽 면(12px) > 정렬(6px) > 데드존(자유) > 그리드(10cm)** — 축별 독립 적용. 대상 범위: 같은 방 또는 근접 500px 내 바닥 가구(표면 적층 자식 제외, 자기 자신·동반 자식 제외). 그룹 드래그는 **그룹 바운즈 기준** 정렬(상대 배치 유지). 미리보기=커밋 계약 그대로(스냅 결과=프리뷰=커밋).
- [x] 가이드 렌더: accent 얇은 점선(수평/수직), DragOverlay 통합.
- [x] Shift 축 잠금(Figma식): 드래그 중 Shift 홀드 시 시작점 기준 이동량 큰 축으로만 직선 이동 — 단일·그룹 공통. 다중 선택 Shift+클릭과는 제스처 단계가 달라 충돌 없음(클릭=선택 토글, 드래그 중 hold=잠금).
- [x] 기존 테스트 1건 의도 반영 수정(순수 그리드 검증은 가구 없는 plan 으로 — 정렬이 상위 우선순위가 된 동작 변화).
- **보류(선택 항목)**: 벽 끝점 드래그의 타 벽 끝점 수평/수직 정렬 가이드 — 시각 피드백 설계와 함께 후속.
- 테스트 7개(엣지/센터 스냅·데드존·다중 대상 구간·축 잠금·우선순위 통합·적층 자식 제외) — 총 227개. 브라우저 검증: 펜던트 드래그 시 소파·콘솔 센터라인(x=2.6) 스냅+세로 가이드 표시, Shift 대각선 드래그 → X만 이동(Y 불변), Esc 원복.

### 26차 (2026-08-25, "3D 인게임 메뉴 — Tab=메뉴+커서 모드")

- [x] **판단: 기존 Tab 커서 모드와 병합** — Tab 을 누르면 포인터 락 해제 + 화면 중앙 인게임 메뉴. 메뉴 표시 중 WASD 이동·Space/E/P 핫키 전부 무시(`walkthrough/menu.ts` 가드 — 키가 눌린 채여도 프레임 루프에서 차단), 마우스로 메뉴·HUD 자유 조작. 닫기 = Tab/클릭/'이동 모드로' 버튼(재락). Esc 도 재락 시도하나 브라우저 락 쿨다운 중엔 무시될 수 있음(문서화) — 기존 "Esc 두 번=2D 복귀"는 메뉴의 '2D 평면도로' 항목과 HUD 버튼으로 대체.
- [x] 메뉴 항목: ▶ 이동 모드로 / 조감도로 전환 / 주간·야간 토글 / 2D 평면도로 / **조작법 안내**(워크스루 WASD·마우스·클릭 조작·E·P·Tab + 조감도 좌드래그 회전·휠 줌·우드래그 팬). 카드형 버튼+호버 피드백.
- [x] 조감도 탭에도 같은 Tab 키로 대칭 미니 메뉴(이 시점에서 워크스루/2D/주야간 + 조감도 조작법), Esc 닫기, 카메라 바에 "TAB 메뉴" 버튼. 메뉴는 캔버스 위 DOM 오버레이 — 클릭·휠이 카메라로 새지 않음(입력 라우팅 원칙). ARCHITECTURE 입력 라우팅 절에 Tab 병합 문서화.
- 테스트 2개(이동·핫키 가드 매트릭스) — 총 229개. 브라우저 검증: 워크스루 메뉴 5항목+조작법 펼침 스크린샷, 조감도 Tab 토글·항목 3+조작법·Esc 닫힘. 포인터 락 재락은 헤드리스 불가 — 실기기 확인 항목에 추가.

### 27차 (2026-08-25, "실도면 업로드 막힘" 리포트 — Matterport 샘플 4종)

- [x] **재현·원인**: 이미지 표시·스케일 선 긋기·3단계·에디터 진입은 기계적으로 전부 동작했음. 실제 원인은 UX 2건 — ① "자동 인식 결과 신뢰도 92%·벽 14개·확인 필요 2곳"이 실업로드에서는 **하드코딩 목업**이라 3단계에 아무것도 로드되지 않음("인식했다는데 로드 안 됨"으로 인식) ② 스케일 단계에서 기준선을 **드래그로 그으라는 캔버스 안내가 없어** 버튼이 왜 비활성인지 알 수 없음.
- [x] **실제 벽 자동 인식 구현**: `wallDetect.ts`(순수 코어 — 이진 격자에서 긴 수평/수직 어두운 런 스캔, gap 이하 끊김 연결, 벽 두께 평행 런 병합, maxLines 긴 선 우선) + `autoTrace.ts`(DOM 어댑터 — object-fit contain 과 동일 매핑으로 캔버스 렌더 → 밝기 이진화, 셀 블록 샘플링으로 1~2px 얇은 벽 보존, 하단 8% 캡션/워터마크 밴드 제외). **개구부 관통 이음**(gap 24px≈0.6m): 창·문으로 조각난 벽을 하나의 중심선으로 — 에디터 모델(벽은 개구부 관통)과 일치. 인식 실패는 빈 결과로 강등(수동 트레이싱 경로 유지).
- [x] UI 정직화: 목업 수치 제거 → 실인식 "벽 후보 N개"(인식 중 표시 포함), 문·창은 "에디터에서 배치" 명시, 안내 카드도 실상태 기반(후보 없음 → 직접 트레이싱 안내). 3단계 진입 시 실업로드도 후보 프리필(샘플과 공통 경로).
- [x] UX 보강: 2단계 캔버스 상단 힌트 오버레이("길이를 아는 벽을 드래그"), 비활성 버튼 아래 사유 문구, 포인터 캡처(캔버스 밖 드롭 처리), 3단계 **선 더블클릭 삭제**(오인식 정리 — 기존엔 삭제 수단 없음).
- 브라우저 실검증(도그푸딩): BW Floor1 — 업로드→인식 36후보(외곽·내벽·개구부 관통 확인)→기준선→3단계 프리필→더블클릭 삭제(36→35)→에디터 진입(36벽+언더레이 정합, 콘솔 에러 0). 4개 PNG 전부: BW F1 36 · BW F2 21 · FCL F1 42 · FCL F2 18 후보. 가구·데크 등 노이즈 일부 포함은 "후보 제안+삭제" UX 로 수용(문서화).
- 테스트 6개(wallDetect 런 스캔·gap·병합·ㅁ자·maxLines) — 총 235개.

### 28차 (2026-08-25, "업로드 즉시 로드" 방침 전환 + 라우팅 재정리 + 워크스루 가드)

- [x] **업로드 = 즉시 로드**: 이미지를 넣으면 곧바로 2D 스케치에 밑그림(언더레이)으로 깔린 새 문서가 열림(파일명=도면 이름, 스케일은 폭 10m 가정 시작 — `underlay.ts`). 27차의 3단계 위저드는 제거하고 스케일 보정·벽 자동 인식을 **에디터 안 '밑그림' 패널의 선택 기능으로 강등**. localStorage 보호: 긴 변 1600px 초과 시 다운스케일+JPEG 재인코딩, 이미지는 plan.tracing 에 저장되어 재열기·JSON 내보내기에 포함(문서화).
- [x] '밑그림' 패널(좌하단, float-panel — 입력 라우팅 자동 커버): 표시 토글·불투명도 슬라이더(코얼레싱)·**스케일 맞추기**(기준선 드래그→실측 입력→언더레이만 리스케일, 벽·가구 불변 — Esc 취소)·**벽 자동 인식**(27차 wallDetect 재사용, 후보 일괄 추가=undo 1회). 권장 순서는 스케일→인식(인식된 벽은 언더레이 스케일 기준).
- [x] 벽 0개 문서 파생 안전화: planBounds 가 tracing 영역 폴백, planCenter 는 planBounds 파생으로 재작성, 3D 는 방 없을 때 중립 바닥 평면 렌더(조감도에서 빈 도면도 표시 가능).
- [x] **라우팅 재정리**(피드백): 원인 — 초기 screen 이 'editor' 하드코딩이라 앱 진입이 마지막 문서의 에디터로 떨어짐. 수정 — **진입점=대시보드(도면 목록+견적)**, 문서 열기/생성/업로드는 항상 2D 스케치 도착(openPlan 기존 동작), 견적보기는 명시 버튼으로만.
- [x] **워크스루 빈 도면 가드**(버그): 방 0개로 진입 시 검은 화면("무한 로드"처럼 보임 — 원인: 걸을 공간·바닥·조명 대상이 없어 렌더 결과가 빈 화면, 로딩이 멈춘 게 아님). 진입 차단 오버레이("아직 걸어볼 공간이 없습니다" + 2D로/조감도로 버튼)로 원천 차단. 스폰 개선: 선택된 방 > 가장 큰 방 중심.
- 브라우저 E2E: 초기 진입=대시보드 → 새 도면 → Matterport PNG 업로드 → **즉시 에디터**(언더레이+밑그림 패널) → 워크스루 탭=가드 화면 → 벽 자동 인식 29후보 추가(undo 안내) → 스케일 맞추기(기준선 120px→4.2m 입력→widthM 10→24.93, 벽 불변). 테스트 4개(underlaySize·rescale·linesToWalls·planBounds 폴백) — 총 239개.

### 29차 (2026-08-25, "즉시 로드 완성판 — 업로드=벽·방 자동 생성, 워크스루 완화, 외곽 폐합")

- [x] **업로드 시 자동 인식 자동 실행**: 버튼 없이 업로드 즉시 wallDetect 실행 → 벽·방이 초기 문서에 포함("넣자마자 벽이 세워진 도면"). 밑그림 패널 '벽 자동 인식'은 재실행 — 이전 자동분('-ad' id)만 교체하고 사용자가 그린 것은 유지(undo 1회 취소).
- [x] **외곽 폐합**(사용자 기준 "테두리 전부 벽"): 세그먼트 후처리 `closeOutline`(코너 스냅+외곽 한정 관대 브리징) + **실루엣 외피**(`silhouetteMask` — 각 행/열의 극값 벽 셀을 인접 연결해 도면 외곽 엔벨로프를 닫힌 곡선으로 봉합). bbox 밴드 휴리스틱만으로는 요철 외곽(데크·베란다)에서 실패해 실루엣 방식 채택 — 인식 누락·개구부가 있어도 둘레가 구조적으로 완결됨.
- [x] **방 자동 생성(best-effort)**: 실루엣+선분 마스크에서 외부 flood 제거 후 닫힌 영역 검출(`detectEnclosedRegions`) → bbox 직사각 근사 폴리곤·실채움 셀 기반 면적, 도면 크기 상대 임계(0.4%·하한)·상위 12개. 한계: L자 공간 폴리곤 과대(면적은 정확), 문으로 이어진 공간이 하나로 묶일 수 있음, 이름은 '공간 N'.
- [x] **스케일 맞추기 의미 승격**: 벽·방이 밑그림 파생이 되면서 "언더레이만 리스케일"(28차 지시)로는 정합이 깨짐 → **문서 지오메트리 전체 배율**(벽·방·가구 위치·치수 주석·언더레이)로 변경, **가구 크기는 실물 치수라 불변**. `rescalePlanGeometry`.
- [x] **워크스루 완화**: 방 없어도 **벽이 있으면 진입 허용**(중립 바닥+벽 충돌, 스폰=도면 중심에서 충돌 밀어내기). 차단 가드는 벽·방 모두 없는 진짜 빈 도면만(`walkthroughAllowed`). 방 있으면 선택 방>최대 방 스폰(기존). 샘플·템플릿 경로는 rooms 보유로 원래 정상 확인.
- [x] **가구 자동 인식 실측 후 불채택**: Matterport BW F1 중간 밝기 성분 529개 중 가구 크기 후보 **3개**(심볼이 서로·벽과 접해 거대 블롭으로 병합, recall ~5% 수준) — 자동 배치 품질 미달로 제안형도 보류. 대안: 언더레이의 가구 심볼 위에 카탈로그 아이템을 얹는 기존 배치 UX 사용(업로드 안내문 명시). 한계 STATUS 기록.
- E2E(Matterport 4종): BW F1 벽29·방12 / BW F2 벽25·방9 / FCL F1 벽32·방12 / FCL F2 벽22·방7 — 전부 외곽 폐합 성립(방 생성이 그 증거), 방 있는 문서 워크스루 진입 확인(가드 미표시·HUD 정상). 테스트 5개(외곽 폐합·실루엣 영역·진입 조건·전체 배율) — 총 244개.

### 15차 (2026-08-25, "창 도구 무반응" 리포트)

- [x] 재현: 벽 정확 클릭은 동작 — **히트 톨러런스 14px을 벗어나면 무반응+피드백 없음**이 원인(사용자 그린 벽 여부와 무관). 수정: ⓐ문/창 클릭·호버 톨러런스 14→24px ⓑ빗나간 클릭에 안내 칩("벽 위를 클릭해 창/문을 배치하세요", 1.8s) ⓒ벽이 개구부 폭보다 짧으면 배치 거부+사유 안내 ⓓ창 아이콘을 벽 단면+창 형태로 교체(테이블로 오독 방지). 브라우저 검증: 사용자 그린 벽에 20px 어긋난 클릭 배치 성공·안내 칩·새 아이콘.

### 13차 (2026-08-24, "벽 선택·편집 + 문 타입 전환 + 히스토리 코얼레싱")

- [x] **벽 선택·편집** (`src/model/wallEdit.ts`): 선택 도구로 벽 클릭 → 선택(하이라이트+끝점 핸들). 끝점 드래그 = 길이/방향 수정(벽 그리기와 동일한 15°/10cm 스냅), 몸통 드래그 = 평행 이동(그리드 스냅), Delete = 벽+개구부+벽부착 연쇄 삭제(room.wallIds 정리). **공유 정점 접합 유지** — 같은 좌표의 다른 벽 끝점·룸 폴리곤 꼭짓점이 함께 이동, 면적 shoelace 재계산. 개구부는 t 비율 유지+끝 재클램프, 벽이 개구부보다 짧아지면 개구부 삭제. 전부 명령 단위 undo.
  - **범위 명시**: ⓐ기존 벽들과의 조합으로 닫힌 공간 자동 룸 인식(topology solver)은 미구현 — 벽 그리기(W)로 닫을 때만 룸 생성. ⓑ템플릿 룸 폴리곤은 안목치수(내벽면)라 중심선 정점과 어긋나 함께 변형되지 않음(샘플·사용자 작성 룸은 동기됨). ⓒT자 접합(끝점이 다른 벽 중간에 닿는 경우)은 미고정. ⓓ벽 다중 선택은 미지원(단일 선택 — 스펙 허용 옵션).
- [x] **문·창 선택 + 속성 패널**: 개구부 클릭 = 선택(패널 표시), 선택된 문 재클릭 = 여닫기 토글. OpeningInspector — 여닫이↔미닫이 전환(doorType), 스윙 방향(여닫이만), 폭 조정(0.5~3.0m + 벽 내 재클램프), 열림 토글, 삭제. 창도 폭 조정·삭제 지원. 전환 시 2D 심볼·3D 메시·doorZones(스윙 존↔스트립)·충돌 전부 파생 재계산(테스트 검증), undo 대상.
- [x] **히스토리 코얼레싱**: updatePlan에 coalesceKey — 같은 키 커밋이 400ms 내 연속이면 히스토리 1개로 묶음(스와치 연타·수치 입력·마감 선택·개구부 속성). undo/redo/제스처 pushHistory가 체인 절단. 드래그류는 기존 pushHistory 1엔트리 패턴 확인, localStorage 2s 디바운스·HISTORY_LIMIT 100 기존 확인.
- 테스트 18개 추가(총 180개): 공유 정점 이동·평행 이동·재클램프·연쇄 삭제·타입 전환 시 doorZones 재계산·코얼레싱(묶기/키 분리/체인 절단).

### 12차 (2026-08-24, "선택 컨트롤에 휴지통 아이콘")

- [x] 선택된 가구 결합 AABB 우상단에 휴지통 버튼(warn 색 원형, 기존 핸들과 동일한 논스케일 스타일). 클릭 = Delete 키와 동일 경로(deleteSelection) — 다중 선택이면 전체 삭제, undo 대상. 브라우저 검증: 클릭 삭제(13→12개)·Cmd+Z 복원.

### 10차 (2026-08-24, "패널 스크롤 시 캔버스 줌" 버그 + 입력 라우팅 룰)

- [x] 원인: `.editor2d` 루트의 wheel 핸들러가 자식 패널의 휠까지 버블로 수신 → 카탈로그 스크롤이 캔버스 줌으로 샘.
- [x] 룰베이스 정리: `inputRouting.ts` — "포인터가 떠 있는 표면이 이벤트의 주인" 원칙. `wheelTargetsCanvas(e.target)` 가드(OVERLAY_SELECTOR: float-panel/tooldock/statusbar/viewtabs/topbar/collision-actions)로 오버레이 위 휠 차단, `.scroll-y`에 `overscroll-behavior: contain`(끝 도달 체이닝 금지). 캔버스 위 휠=커서 기준 줌(README 준수, 핀치 포함) 유지. 3D는 구조상 안전(워크스루 wheel 미사용, 조감도 OrbitControls는 gl canvas 요소에만 리스너) — window 단위 wheel 리스너 없음 확인.
- [x] **ARCHITECTURE.md "입력 라우팅 규칙" 절 문서화** — 새 패널은 OVERLAY_SELECTOR 등록 + .scroll-y 사용 규칙 명시.
- 테스트 4개(가드 로직·셀렉터 등록) — 총 150개. 브라우저 실검증: 카탈로그 위 휠 5회 → 축척 1:55 불변, 캔버스 위 휠 → 1:20 줌.

### 버그 리포트 (2026-08-24, "가구 색상 변경·세부옵션 미동작")

- [x] 재현 결과: 일반 가구(소파 등)의 스와치는 2D/3D 모두 정상 — **조명류(플로어 램프·펜던트)만 2D 심볼·3D 메시가 variant 색을 무시하고 고정 팔레트를 사용**해 스와치가 무반응으로 보였음. 또한 '상세 옵션' 버튼이 스텁(무동작)이었음.
- [x] 수정: `lampPartColors` 공용 헬퍼 도입 — 마감 스와치는 몸체(폴·베이스·프레임·허브), 발광부는 warm 고정. 2D 심볼·3D 메시 배선 통일. '상세 옵션' 기능화: 마감 라벨 선택 리스트 + 전원 토글(조명·TV) + 복제/삭제. 3D E-편집 패널에도 전원 토글 추가. 회귀 테스트 2개(색 배선). 브라우저 재검증 완료.

## 현재 상태

- 프로젝트: `/Users/kyb-ontact/sonix/toy/roomcast` (git 초기화됨, 리모트 없음)
- `npm run build` / `npm test`(62개) **통과** ✅
- 스택: React 18.3 + TS + Vite 8 / three 0.170 + r3f 8 + drei 9 / zustand 5
- SSOT `Plan` 모델·카탈로그·샘플 도면·지오메트리 유틸·스토어(undo/localStorage 저장) 완성
- 화면 셸: 상태 기반 라우팅(dashboard/editor/upload × 2d/walkthrough/birdseye), 상단바·뷰 탭 구현

## Questions (합리적 가정으로 진행 중)

- **React 버전**: Vite 스캐폴드는 React 19였으나 지시대로 React 18.3으로 고정.
  r3f는 v8, drei는 v9 (React 18 호환 최종 메이저), three는 drei 9 peer 범위인 0.170.
- **룸 면적**: 핸드오프의 룸 라벨 값(거실 31.2㎡ 등)은 도면 지오메트리와 불일치
  (합계도 84㎡와 안 맞음). 지오메트리에서 shoelace로 **자동 계산한 실제 값**을 표기함.
- **Room 타입**: README 타입에 `polygon` 필드를 추가(렌더·판정용). ARCHITECTURE.md 참조.
- **상태관리**: README에 라이브러리 지정이 없어 zustand 채택 (r3f 표준 조합).

- **치수 도구**: Plan 타입에 치수 주석 필드가 없어(README 준수) 치수 도구는
  드래그 측정(비영속) + 도면 외곽 자동 치수선으로 구현. 영속 치수 주석은 후속 과제.
- **1c-4 "42cm 겹침" 수치**: 겹침 깊이 계산 대신 겹친 가구명 표기로 대체.

## 로그

- 2026-08-24: 프로젝트 생성, 단계 ① 완료.
- 2026-08-24: 단계 ② 완료 — 2D 에디터 전체 인터랙션 + 브라우저 스모크 테스트(선택·드래그·스냅·충돌 확인).
- 2026-08-24: 단계 ③④⑤⑥ + Vitest 테스트 43개 완료. **전 단계 완료** — `npm run build`·`npm test` 통과.
- 남은 확인: 3D 실렌더(워크스루·조감도)는 WebGL 되는 실기기에서 육안 확인 필요 (헤드리스는 SwiftShader 실패로 폴백 표시됨).
